import { IndianCurrencyDirective } from './indian-currency.directive';

describe('IndianCurrencyDirective', () => {
  it('should create an instance', () => {
    const directive = new IndianCurrencyDirective();
    expect(directive).toBeTruthy();
  });
});
